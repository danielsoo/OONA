import BrowseTabs from "@/components/browse/BrowseTabs";

/** Tabs above the legacy showcase pages that only render in demo mode. */
export default function DemoBrowseTabs() {
  return (
    <div className="px-4 pt-4 lg:px-12">
      <BrowseTabs />
    </div>
  );
}
